# Replay Service

Сервис для хранения и управления игровыми реплеями с REST API и веб-интерфейсом.

## Возможности

- 🎮 Управление играми (создание, удаление)
- 📁 Загрузка и хранение реплеев
- 💾 Скачивание реплеев
- 🗂️ Группировка реплеев по играм
- 👤 Мультипользовательская поддержка
- 🌐 Простой веб-интерфейс

## Быстрый старт

### Запуск

```bash
chmod +x start.sh  
./start.sh
```

Сервис будет доступен:
- API: `http://localhost:8080/api/v1`
- Веб-интерфейс: `client/index.html` (открыть в браузере)

### Остановка

```bash
sudo docker compose -f deploy/docker-compose/docker-compose.yml down
```

### Остановка с удалением данных

```bash
sudo docker compose -f deploy/docker-compose/docker-compose.yml down -v
```

## Структура проекта

```
replay-service/
├── server/                    # Backend (Go)
│   ├── cmd/replay-service/   # Точка входа
│   ├── config/               # Конфигурация
│   ├── internal/             # Внутренняя логика
│   │   ├── database/        # Подключение к БД
│   │   ├── handlers/        # HTTP handlers
│   │   ├── middleware/      # Middleware
│   │   ├── models/          # Модели данных
│   │   └── repository/      # Работа с БД
│   └── migrations/          # SQL миграции
├── client/                   # Frontend (HTML)
│   └── index.html           # Веб-интерфейс
├── storage/                  # Файловое хранилище
├── deploy/                   # Docker конфигурация
├── docs/                     # Документация
└── start.sh                 # Скрипт запуска
```

## Технологии

- **Backend:** Go 1.24, Gin, PostgreSQL
- **Frontend:** Vanilla JavaScript, HTML, CSS
- **Deployment:** Docker, Docker Compose
- **Storage:** Файловая система хоста

## Документация

- [API Specification](docs/api-specification.md) - описание REST API
- [Storage Structure](docs/storage-structure.md) - структура файлового хранилища
- [API Examples](docs/api-examples.http) - примеры запросов

## Примеры использования

### Веб-интерфейс

1. Откройте `client/index.html` в браузере
2. Создайте игру через кнопку "+ Добавить"
3. Выберите игру из списка
4. Загрузите реплей через форму

### API

```bash
# Получить список игр
curl -H "X-User-ID: 00000000-0000-0000-0000-000000000001" \
  http://localhost:8080/api/v1/games

# Создать игру
curl -X POST http://localhost:8080/api/v1/games \
  -H "X-User-ID: 00000000-0000-0000-0000-000000000001" \
  -H "Content-Type: application/json" \
  -d '{"name":"Dota 2"}'

# Загрузить реплей
curl -X POST http://localhost:8080/api/v1/games/{game_id}/replays \
  -H "X-User-ID: 00000000-0000-0000-0000-000000000001" \
  -F "file=@replay.rep" \
  -F "title=Epic game" \
  -F "comment=My best match"
```

## База данных

### Структура

- `users` - пользователи
- `games` - игры пользователей
- `replays` - реплеи с метаданными

### Миграции

Миграции применяются автоматически при запуске через Docker Compose.

## Хранилище файлов

Файлы хранятся в `./storage/` на хосте:

```
storage/
└── {user_id}/
    └── {game_id}/
        └── {replay_id}.ext
```

Подробнее: [Storage Structure](docs/storage-structure.md)

## Разработка

### Требования

- Go 1.24+
- Docker & Docker Compose
- PostgreSQL 16

### Локальный запуск

```bash
# Установить зависимости
go mod download

# Запустить БД
docker compose -f deploy/docker-compose/docker-compose.yml up postgres

# Применить миграции
migrate -path server/migrations -database "postgres://..." up

# Запустить сервер
go run server/cmd/replay-service/main.go
```

### Переменные окружения

Создайте `.env` файл:

```env
PORT=8080
DB_DSN=postgres://replay:replay@localhost:5431/replay?sslmode=disable
STORAGE_DIR=./storage
LOG_LEVEL=debug
```

## Лицензия

MIT
